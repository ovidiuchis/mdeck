###### Components · steps

## Three steps to a deck

::: steps
1. **Create** a folder in `presentations/` with a `presentation.json`.
2. **Write** one `.md` file per slide — text, code and components.
3. **Present** with [[→]] / [[Space]], `G` for overview, `F` for fullscreen.
:::

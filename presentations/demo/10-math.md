###### Rich content · math

## Formulas with KaTeX

Inline math like $e^{i\pi} + 1 = 0$ flows with the text, and display blocks stand on their own:

$$
\sigma = \sqrt{\frac{1}{N}\sum_{i=1}^{N}(x_i - \mu)^2}
$$

Rendered locally — KaTeX and its fonts are vendored, so math works fully offline. :check:
